name: Stage the app

on:
  pull_request:
    types: [labeled]

jobs:
  build:
    runs-on: ubuntu-latest
    # Filters the build job when there is a label present called stage
    if: contains(github.event.pull_request.labels.*.name, 'stage')
