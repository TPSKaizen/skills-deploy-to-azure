name: Stage the app

# This workflow is to trigger on pull requests that have a certain label
on:
    pull_requests:
        types: [labeled]

jobs:
    build:
        runs-on: ubuntu-latest

        if: contains(github.event.pull_request.labels.*.name, 'stage')